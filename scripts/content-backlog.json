{
  "_comment": "Content ladder for the autonomous content runs. Each run builds the first entry whose status is 'todo', wires it to be reachable in play, passes the self-verify gate (scripts/selfplay.mjs + size-limit), then flips it to 'done'. Never repeat a done entry.",
  "ladder": [
    {
      "id": "blue-tier",
      "priority": 1,
      "status": "done",
      "summary": "Populate lj.enemy.types.blue {a,b,c,d,B} (frost/arcane theme) and add lj.enemy.familyForRealm(size) so realm 2+ spawns the blue family. Reuses existing a/b/c/d/B tile symbols (single-row sprite sheet has no per-color art).",
      "built": "2026-07-12"
    },
    {
      "id": "green-tier",
      "priority": 2,
      "status": "done",
      "summary": "Populate lj.enemy.types.green {a,b,c,d,B} (\"The Blightwood\" poison/nature theme) and extend the familyForRealm ladder so realm 3+ spawns green. familyLadder auto-picks up the populated family; realm 3=green, realm 4+ clamp to green. Step over blue kept tiny (<=1.17x agility/HP, armor untouched) because the realm-3+ clamp compounds hard with 1.4^(size-1) scaling.",
      "built": "2026-07-13"
    },
    {
      "id": "red-tier",
      "priority": 3,
      "status": "done",
      "summary": "Populate lj.enemy.types.red {a,b,c,d,B} (\"The Emberdeep\" molten/infernal theme) so the familyLadder auto-routes realm 4+ to red. Reuses a/b/c/d/B tile symbols (already in spriteMap + creaturesAndItemsMap). Red now owns realm 4+ — which selfplay shows is essentially the whole decisive game (nearly every win/death) — so green retreats to realm 3 alone and the green->red step is deliberately gentle: armor UNTOUCHED on every grade (it is a % dmg-reduction that compounds hardest with depth), escalation is +1 HP/grade plus +1 agility on striker+boss and +1 boss crit. A full notch (flat +1 str on striker+boss) cratered the sim to ~24% (below the 25% floor); this holds ~28-29%, a clean ~4pt step below green mirroring blue->green.",
      "built": "2026-07-13"
    },
    {
      "id": "new-boss-mechanic",
      "priority": 4,
      "status": "done",
      "summary": "Added The Cinderwyrm (red grade \"T\"), the first boss with a real combat mechanic: ENRAGE. lj.enemy.monster carries an optional mechanic descriptor; lj.enemy.base copies it onto instances; lj.enemy.enraged(enemy,curHp,maxHp) returns a boosted attack snapshot below threshold*maxHp and the fight loop (lj.hero.fight in combat.js) resolves the enemy's swing through it, so damage ramps mid-fight. Reachable: lj.enemy.bossGradeForRealm(size) -> \"T\" at realm 5+ (else \"B\"), placed in realm.js, routed in hero.js (creaturesAndItemsMap + lj.enemy.isBoss level-up), rendered via scene.js spriteMap (reuses boss sprite [0,9]); selfplay fights the per-realm boss grade. Tuned as a risk/reward trade: base offense [5,7] gentler than the Emberlord's [7,8], enrage (0.4 threshold, 1.5x str&agi) makes the sub-40% back half hit ~[8,11] — harder than the old boss. Net ~29.3% win rate (A/B: enrage off 33.5% vs on 28.4%, ~5pt swing => load-bearing).",
      "built": "2026-07-13"
    },
    {
      "id": "affix-batch",
      "priority": 5,
      "status": "done",
      "summary": "Added the 'Spoils of the Realms' loot affix batch to js/stats.js: 4 prefixes (Frostforged, Venomous, Emberforged, Wyrmscaled) + 4 suffixes (of the Frostmarch, of the Blightwood, of the Emberdeep, of the Cinderwyrm), one prefix+suffix pair per enemy family (blue/green/red/apex) so the loot pool mirrors the tiers the hero fights. Auto-reachable: lj.items.makeItem picks rng(0,len-1) across the full prefixes/suffixes arrays and applies each ~30% per item, so pushing onto the arrays is the only wiring needed; affixes never render as map tiles, so scene.js spriteMap / hero.js creaturesAndItemsMap are untouched (zero symbol-crash risk). BALANCE GOTCHA: the gScore auto-equip weights every stat equally, so an armor/defense-heavy affix is picked over an equal-scored offensive one and quietly dilutes the hero's damage — an armor-tilted first draft dropped a 500-run sample to 24.4% (below the 25% floor). Reweighted the batch offense/HP-first (the stats that actually convert to boss-attrition wins) and it lands balance-neutral: isolated A/B (worktree off clean origin/main) measured baseline 29.1% vs batch 29.6% at 3000 runs each (+0.5pt, within noise), and the 6000-run --check gate passed at 27.6%; pooled ~28% over 9000 batch runs. luck (index 2) is inert in combat and gScore so it's 0 in every new affix (nonzero would pad).",
      "built": "2026-07-13"
    },
    {
      "id": "room-template-biome",
      "priority": 6,
      "status": "done",
      "summary": "Added a BIOME system to js/realm.js: every realm's rooms share one depth-themed architecture, mapped to mirror the enemy-family ladder (r1 halls, r2 frost 'Frostmarch', r3 blight 'Blightwood', r4+ ember 'Emberdeep' — biomeForRealm clamps like familyForRealm). The sprite sheet is a single 16-tile row with NO spare art (assets/sprite.png is 512x32; col15 is already the dead-hero sprite), so biomes can't have new floor/wall art — instead each biome RE-WEIGHTS the base bomberman offshoot rule via a new placeOffshoot(): halls = uniform (reproduces the original generator), frost = vertical-biased colonnades, blight = lateral-biased warren, ember = sparse sprout (0.55) open caverns. Reachable in play: makeRealm locks in the biome and every makeRoom uses it (verified in-browser via scene.reset()+levelUp() across realms 1-6 — all biomes render, zero console/thrown errors). spriteMap coverage: biomes only ever emit '#'/' ' (both already covered), so scene.js/hero.js are UNTOUCHED and there is zero symbol-crash risk. BALANCE: neutral BY CONSTRUCTION — selfplay's playthrough() abstracts the spatial layer and never calls makeRoom, so the win rate is unchanged (28.4% at 6000 runs vs 28.2% baseline, well inside [25,75]). The load-bearing NEW gate for this structural module is a room-connectivity + symbol validator added to scripts/selfplay.mjs (--rooms, and folded into --check): it flood-fills every generated room from the hero spawn [5,9] and asserts a single walkable component + only known symbols. CONNECTIVITY is preserved by construction — every pillar still sprouts AT MOST ONE wall and pass-2 walls never grow 'west' (keeps the wall-forest acyclic); placeOffshoot has a bounded retry + guaranteed-free escape cell so generation always terminates. GOTCHA for future room work: a naive denser biome that adds >1 wall/pillar or lets pass-2 sprout west CAN wall off a region — keep the ≤1-offshoot + no-west invariant, or the --rooms gate will (correctly) fail.",
      "built": "2026-07-14"
    },
    {
      "id": "targeted-rebalance",
      "priority": 7,
      "status": "done",
      "summary": "Data-driven rebalance pass. The win rate had drifted to the LOW band edge — selfplay --check read ~28% (only ~3pt over the 25% floor), a fragile margin. Per-realm diagnostics showed realms 5-7 own nearly every win and death, ~70%+ of losses are boss attrition, and HALF of all boss deaths landed with the boss already under 20% HP (median 19.7% left): the hero loses razor-thin, a hair short on damage/effective-HP. Chose the single most load-bearing, best-behaved lever: the global realm-scaling exponent in lj.enemy.base (js/enemy.js), realmMod = (1 + exp)^(size-1). A sweep was clean and monotonic (0.40->28%, 0.39->31.5%, 0.385->34%, 0.38->37%). Eased 0.40 -> 0.375, which the shipped `node scripts/selfplay.mjs --runs 5000 --check` measured at 37.2% (1859/5000) IN-BAND, beatable+survivable, structural gate green (26,400 rooms). Why the exponent over a hero buff: hero GEAR is unscaled (items default to itemLevel 1) while enemies scale with depth, so a flat hero-base buff mostly eases realm 1-3 (where ~0 deaths happen); the exponent's effect is inherently back-loaded to the deep realms where the game is actually decided, AND it eases the compounding armor/HP the tier comments call the depth villain — while preserving every tier's RELATIVE step (all families scale by the same factor). Restrained on purpose: 37% roughly quadruples the floor margin (3->12pt) yet keeps a sub-40% 'hard' roguelike, far from the 75% ceiling; not an overshoot to 50%. At size 1 the exponent is inert (x^0=1) so realm 1 is byte-identical. Also refreshed two now-stale '1.4^(size-1)' code comments to 1.375. Independent audit confirmed: single-point knob, no degenerate enemies (min boss HP stays >=15), no loot/render/structural coupling.",
      "built": "2026-07-14"
    },
    {
      "id": "boss-mechanic-2",
      "priority": 8,
      "status": "todo",
      "summary": "Add a SECOND distinct boss combat mechanic (the Cinderwyrm's enrage was the first), on a different family boss so a deep run faces more than one boss identity. Follow the enrage pattern exactly: a mechanic descriptor on lj.enemy.monster copied through lj.enemy.base, resolved inside the fight loop (js/combat.js) via a small helper like lj.enemy.enraged. Candidate: a 'Warding'/'Shield' mechanic that periodically absorbs a hit, or a 'Frost Nova' that reduces hero accuracy for a turn — something that changes the fight's SHAPE, not just its numbers. Reachability: route via lj.enemy.bossGradeForRealm (add a new grade, e.g. green apex at realm 3, or blue apex) and add the grade to lj.enemy.isBoss + hero.js creaturesAndItemsMap + scene.js spriteMap (reuse the existing boss sprite tile like grade 'T' did — the sprite sheet has no spare art). Gate: keep selfplay --check in-band; the mechanic must be load-bearing (A/B on/off should move the win rate), balanced against a gentler base like the Cinderwyrm was."
    },
    {
      "id": "affix-batch-2",
      "priority": 9,
      "status": "todo",
      "summary": "Add a second coherent loot-affix batch to js/stats.js (4 prefixes + 4 suffixes) with a fresh unifying theme distinct from 'Spoils of the Realms' (e.g. gladiator/arena or elemental-attunement). Auto-reachable: lj.items.makeItem draws rng across the full prefixes/suffixes arrays, so pushing onto them is the only wiring; affixes never render as tiles (no scene.js/hero.js changes, zero symbol-crash risk). BALANCE GOTCHA carried forward: gScore (combat.js) weights every stat equally, so armor/defense-heavy affixes auto-equip over equal-scored offense and DILUTE the hero's boss-attrition damage — keep the batch offense/HP-first, and luck (index 2) is inert so leave it 0. Verify balance-neutral via an isolated A/B (worktree off clean origin/main) plus the --check band."
    }
  ]
}
